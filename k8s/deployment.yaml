---
# GoZero Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gozero
  namespace: mix-web-demo
  labels:
    app: gozero
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gozero
  template:
    metadata:
      labels:
        app: gozero
        version: v1
    spec:
      containers:
        - name: gozero
          image: mix-gozero:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              name: http
          env:
            - name: SERVER_IP
              value: "0.0.0.0"
            - name: SERVER_PORT
              value: "8082"
            - name: NACOS_IP
              value: "nacos"
            - name: NACOS_PORT
              value: "8848"
          volumeMounts:
            - name: config
              mountPath: /app/etc/application.yaml
              subPath: application.yaml
            - name: logs
              mountPath: /app/logs/gozero
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: gozero-config
        - name: logs
          emptyDir: {}

---
# GoZero Service
apiVersion: v1
kind: Service
metadata:
  name: gozero
  namespace: mix-web-demo
  labels:
    app: gozero
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: gozero

---
# NestJS Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nestjs
  namespace: mix-web-demo
  labels:
    app: nestjs
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nestjs
  template:
    metadata:
      labels:
        app: nestjs
        version: v1
    spec:
      containers:
        - name: nestjs
          image: mix-nestjs:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
              name: http
          env:
            - name: NODE_ENV
              value: "production"
            - name: SERVER_PORT
              value: "8083"
            - name: NACOS_SERVER
              value: "nacos"
          volumeMounts:
            - name: config
              mountPath: /app/application.yaml
              subPath: application.yaml
            - name: logs
              mountPath: /app/logs/nestjs
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: nestjs-config
        - name: logs
          emptyDir: {}

---
# NestJS Service
apiVersion: v1
kind: Service
metadata:
  name: nestjs
  namespace: mix-web-demo
  labels:
    app: nestjs
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: nestjs
      protocol: TCP
      name: http
  selector:
    app: nestjs

---
# Spring Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring
  namespace: mix-web-demo
  labels:
    app: spring
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring
  template:
    metadata:
      labels:
        app: spring
        version: v1
    spec:
      containers:
        - name: spring
          image: mix-spring:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC"
            - name: NACOS_SERVER
              value: "nacos:8848"
          volumeMounts:
            - name: config
              mountPath: /app/application.yaml
              subPath: application.yaml
            - name: logs
              mountPath: /app/logs/spring
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: spring-config
        - name: logs
          emptyDir: {}

---
# Spring Service
apiVersion: v1
kind: Service
metadata:
  name: spring
  namespace: mix-web-demo
  labels:
    app: spring
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: spring

---
# Gateway Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: mix-web-demo
  labels:
    app: gateway
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
        version: v1
    spec:
      containers:
        - name: gateway
          image: mix-gateway:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: JAVA_OPTS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC"
            - name: NACOS_SERVER
              value: "nacos:8848"
          volumeMounts:
            - name: config
              mountPath: /app/application.yaml
              subPath: application.yaml
            - name: logs
              mountPath: /app/logs/gateway
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: gateway-config
        - name: logs
          emptyDir: {}

---
# Gateway Service
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: mix-web-demo
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: gateway

---
# FastAPI Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi
  namespace: mix-web-demo
  labels:
    app: fastapi
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
        version: v1
    spec:
      containers:
        - name: fastapi
          image: mix-fastapi:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
              name: http
          env:
            - name: SERVER_IP
              value: "0.0.0.0"
            - name: SERVER_PORT
              value: "8084"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: NACOS_SERVER
              value: "nacos:8848"
          volumeMounts:
            - name: config
              mountPath: /app/application.yaml
              subPath: application.yaml
            - name: logs
              mountPath: /app/logs/fastapi
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /docs
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /docs
              port: 8084
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: fastapi-config
        - name: logs
          emptyDir: {}

---
# FastAPI Service
apiVersion: v1
kind: Service
metadata:
  name: fastapi
  namespace: mix-web-demo
  labels:
    app: fastapi
spec:
  type: ClusterIP
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
      name: http
  selector:
    app: fastapi
